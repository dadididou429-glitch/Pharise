# Pharis v2

Global open-pharmacy finder. Vite + React.

## Develop

```bash
npm install
npm run dev
```

## Build for GitHub Pages

```bash
npm run build
```

Deploy the `dist/` folder to GitHub Pages (repo `Pharise`, base `/Pharise/`).

Or use Actions to publish `dist` on every push to `main`.
